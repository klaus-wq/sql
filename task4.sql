1. SELECT *  FROM Employees ORDER BY BirthDate DESC, Country;
2. SELECT *  FROM Employees WHERE Region IS NOT NULL ORDER BY BirthDate DESC, Country;
3. SELECT AVG(UnitPrice), MIN(UnitPrice), MAX(UnitPrice) FROM [Order Details];
4. SELECT COUNT(DISTINCT City) FROM Customers;